# Decisions (ADRs)

One small file per significant decision. Copy `adr-TEMPLATE.md`, number it in
sequence (`adr-001-...`, `adr-002-...`), and keep it short. The point is that the
reasoning -- especially the alternatives you rejected -- survives past the
conversation where you made the call.
